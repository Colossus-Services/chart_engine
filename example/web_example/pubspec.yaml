name: web_example
description: A simple Web Example with `chart_engine`.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  chart_engine: ^1.0.0
    #path: ../../

dev_dependencies:
  build_runner: ^1.10.0
  build_web_compilers: ^2.11.0
  pedantic: ^1.9.0
